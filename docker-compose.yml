services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: dataforgetest-backend
    ports:
      - "5000:5000"
    environment:
      # Flask Settings
      - FLASK_ENV=production
      - FLASK_DEBUG=False

      # CORS — required whenever FLASK_ENV=production. There is no built-in
      # production default any more, so this must be set explicitly or the
      # app refuses to start.
      - CORS_ALLOWED_ORIGINS=${CORS_ALLOWED_ORIGINS:-http://localhost:3000}

      # LLM Configuration
      # Choose provider: 'ollama' (default, no cost) or 'anthropic' (requires credits)
      - LLM_PROVIDER=${LLM_PROVIDER:-ollama}
      
      # Ollama Settings (for open-source LLM)
      - LLM_MODEL=${LLM_MODEL:-qwen2.5-coder:7b}
      - OLLAMA_BASE_URL=${OLLAMA_BASE_URL:-http://ollama:11434}
      
      # Anthropic Settings (optional, only if LLM_PROVIDER=anthropic)
      - LLM_API_KEY=${LLM_API_KEY:-}
      
      # RAG System
      - VECTOR_STORE_PATH=./storage/vectorstore
      - CHUNK_SIZE=${CHUNK_SIZE:-512}
      - CHUNK_OVERLAP=${CHUNK_OVERLAP:-50}
      - TOP_K=${TOP_K:-4}

      # Upload limits. MAX_UPLOAD_MB is the single value every feature module
      # reads; the hard Flask wall is derived from it plus the overhead margin.
      - MAX_UPLOAD_MB=${MAX_UPLOAD_MB:-10}
      - UPLOAD_OVERHEAD_MB=${UPLOAD_OVERHEAD_MB:-2}

      # Diagnostics — /api/rag/debug discloses provider, key presence and paths.
      - RAG_DEBUG_ENDPOINT=${RAG_DEBUG_ENDPOINT:-false}
      
      # Synthetic Data Generation
      - SYNTH_STORAGE_PATH=./storage/synth
      - SYNTH_MAX_ROWS=${SYNTH_MAX_ROWS:-1000000}
      - SYNTH_REQUEST_TIMEOUT=${SYNTH_REQUEST_TIMEOUT:-300}
      - SYNTH_MAX_MEM_MB=${SYNTH_MAX_MEM_MB:-2048}
      
      # Data Accuracy
      - ACCURACY_STORAGE_PATH=./storage/accuracy
      # NOTE: ACCURACY_MAX_UPLOAD_MB used to be set here but no code ever read
      # it — the accuracy module reads MAX_UPLOAD_MB like every other module.
      - ACCURACY_MAX_ROWS=${ACCURACY_MAX_ROWS:-2000000}
      - ACCURACY_REQUEST_TIMEOUT=${ACCURACY_REQUEST_TIMEOUT:-120}
      
      # GOLD Dataset Testing
      - GOLD_STORAGE_PATH=./storage/gold
      - GOLD_ALLOWED_FILE_TYPES=${GOLD_ALLOWED_FILE_TYPES:-.csv,.xlsx,.xls,.parquet}
      - MAX_ROWS_WARN=${MAX_ROWS_WARN:-500000}
      - GOLD_REQUEST_TIMEOUT=${GOLD_REQUEST_TIMEOUT:-300}
    volumes:
      # Persist storage data
      - ./storage:/app/storage
      # Persist uploaded files
      - ./uploads:/app/uploads
    depends_on:
      - ollama
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import requests; requests.get('http://localhost:5000/', timeout=5)"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

  ollama:
    image: ollama/ollama:latest
    container_name: dataforgetest-ollama
    ports:
      - "11434:11434"
    volumes:
      - ollama_data:/root/.ollama
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "ollama", "list"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

volumes:
  storage:
  uploads:
  ollama_data:
